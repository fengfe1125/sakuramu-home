{
  "name": "sakuramu-home",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "snapshot": "node scripts/refresh-snapshot.mjs",
    "deploy": "npm run snapshot && wrangler deploy",
    "deploy:about": "wrangler deploy --config wrangler.v2.jsonc",
    "versions": "wrangler versions list"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
